// Generated by CoffeeScript 1.10.0
var ChartMixin, React, ScatterChart, d3;

React = require('react');

d3 = require('d3');

ChartMixin = require('./common');

module.exports = ScatterChart = React.createClass({displayName: "ScatterChart",
  mixins: [ChartMixin],
  getDefaultProps: function() {
    return {
      color: '#000',
      curve: true,
      fill: true
    };
  },
  render: function() {
    var data, height, ref, width, x, x_extent, y, y_extent;
    ref = this.props, width = ref.width, height = ref.height, data = ref.data, x = ref.x, y = ref.y;
    x_extent = d3.extent(data, function(d) {
      return d.x;
    });
    y_extent = d3.extent(data, function(d) {
      return d.y;
    });
    x || (x = d3.scaleLinear().domain(x_extent).range([0, width]));
    y || (y = d3.scaleLinear().domain([0, y_extent]).range([height, 0]));
    return React.createElement("svg", {
      "className": 'scatter-chart',
      "style": {
        width: width,
        height: height
      }
    }, data.map(function(d, i) {
      return React.createElement("circle", {
        "className": 'dot',
        "r": 3,
        "key": i,
        "cx": x(d.x),
        "cy": y(d.y),
        "fill": d.color
      });
    }));
  }
});
