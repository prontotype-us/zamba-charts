// Generated by CoffeeScript 1.10.0
var React;

React = require('react');

exports.XAxis = React.createClass({displayName: "XAxis",
  render: function() {
    var height, ref, width, x;
    ref = this.props, width = ref.width, height = ref.height, x = ref.x;
    return React.createElement("svg", {
      "className": 'axis x-axis',
      "style": {
        width: width,
        height: height,
        position: 'absolute',
        bottom: 0,
        left: padding
      }
    }, x.ticks(10).map(function(t, ti) {
      return React.createElement("text", {
        "x": x(t),
        "y": height - 6,
        "textAnchor": 'middle',
        "key": ti
      }, t.toFixed(0));
    }));
  }
});

exports.YAxis = React.createClass({displayName: "YAxis",
  render: function() {
    var height, ref, width, y;
    ref = this.props, width = ref.width, height = ref.height, y = ref.y;
    return React.createElement("svg", {
      "className": 'axis y-axis',
      "style": {
        width: width,
        height: height,
        position: 'absolute',
        left: 0,
        top: padding
      }
    }, y.ticks(height / 20).map(function(t, ti) {
      return React.createElement("text", {
        "y": y(t) + 6,
        "x": width / 2,
        "textAnchor": 'middle',
        "key": ti
      }, t.toFixed(2));
    }));
  }
});
