// Generated by CoffeeScript 1.8.0
var React;

React = require('preact');

exports.XAxis = function(_arg) {
  var axis_size, border, format, height, label, labels, padding, position, rotate, style, ticks, width, x;
  width = _arg.width, height = _arg.height, axis_size = _arg.axis_size, x = _arg.x, padding = _arg.padding, position = _arg.position, format = _arg.format, ticks = _arg.ticks, labels = _arg.labels, label = _arg.label, border = _arg.border, rotate = _arg.rotate;
  padding || (padding = 0);
  style = {
    width: width,
    height: axis_size,
    position: 'absolute',
    left: 0,
    top: height
  };
  if (position === 'bottom') {
    style.bottom = padding;
  } else {
    style.top = padding;
  }
  return React.createElement("svg", {
    "className": 'axis x-axis',
    "style": style,
    "height": axis_size,
    "width": width
  }, (labels != null ? React.createElement("g", {
    "className": 'labels'
  }, Object.keys(labels).map(function(label_x) {
    return React.createElement("text", {
      "x": x(label_x),
      "y": 0,
      "text-anchor": 'middle',
      "key": label_x
    }, labels[label_x]);
  })) : x.ticks(ticks || 10).map((function(_this) {
    return function(t, ti) {
      var alignment_baseline, text_anchor, text_y, tick_label;
      tick_label = format != null ? format(t) : t.toFixed(0);
      text_y = position === 'bottom' ? axis_size : 0;
      alignment_baseline = position === 'bottom' ? 'baseline' : 'hanging';
      if (rotate > 0) {
        text_anchor = 'start';
      } else if (rotate < 0) {
        text_anchor = 'end';
      } else {
        text_anchor = 'middle';
      }
      return React.createElement("text", {
        "x": x(t),
        "y": text_y,
        "text-anchor": text_anchor,
        "key": ti,
        "alignmentBaseline": alignment_baseline,
        "transform": "rotate(" + rotate + "," + (x(t)) + "," + text_y + ")"
      }, tick_label);
    };
  })(this))), (label ? React.createElement("text", {
    "className": 'label',
    "x": width,
    "y": 0,
    "key": 'label',
    "style": {
      fontWeight: 'bold'
    }
  }, label) : void 0), (border ? React.createElement("rect", {
    "height": axis_size,
    "x": axis_size,
    "width": width - axis_size
  }) : void 0));
};

exports.YAxis = function(_arg) {
  var axis_size, border, format, height, label, labels, labels_width, padding, position, style, ticks, width, y;
  width = _arg.width, height = _arg.height, axis_size = _arg.axis_size, y = _arg.y, padding = _arg.padding, position = _arg.position, format = _arg.format, ticks = _arg.ticks, label = _arg.label, labels = _arg.labels, border = _arg.border;
  padding || (padding = 0);
  style = {
    width: axis_size,
    height: height,
    position: 'absolute',
    top: 0
  };
  if (position === 'left') {
    style.left = padding;
  } else {
    style.right = padding;
  }
  labels_width = 20;
  return React.createElement("svg", {
    "className": 'axis y-axis',
    "style": style,
    "height": height,
    "width": width
  }, React.createElement("g", {
    "className": 'labels',
    "transform": "translate(-" + labels_width + ",0)"
  }, (labels != null ? Object.keys(labels).map(function(label_y) {
    return React.createElement("text", {
      "y": y(label_y),
      "x": labels_width - 5,
      "text-anchor": 'end',
      "key": label_y
    }, labels[label_y]);
  }) : y.ticks(ticks || (height / 20)).map(function(t, ti) {
    var text_anchor, text_x, tick_label;
    tick_label = format != null ? format(t) : t.toFixed(0);
    text_x = position === 'left' ? 0 : width;
    text_anchor = position === 'left' ? 'start' : 'end';
    return React.createElement("text", {
      "y": y(t),
      "x": text_x,
      "text-anchor": text_anchor,
      "alignmentBaseline": 'middle',
      "key": ti
    }, tick_label);
  }))), (label != null ? React.createElement("text", {
    "className": 'label',
    "y": 0,
    "text-anchor": 'start',
    "key": 'label',
    "style": {
      fontWeight: 'bold'
    }
  }, label) : void 0), (border ? React.createElement("rect", {
    "width": axis_size,
    "x": 0,
    "y": 0,
    "height": height
  }) : void 0));
};
