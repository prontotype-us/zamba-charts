// Generated by CoffeeScript 1.10.0
var Chart, LineChart, React, d3, helpers,
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty;

React = require('react');

d3 = require('d3');

Chart = require('./chart');

helpers = require('./helpers');

module.exports = LineChart = (function(superClass) {
  extend(LineChart, superClass);

  function LineChart() {
    return LineChart.__super__.constructor.apply(this, arguments);
  }

  LineChart.prototype.renderChart = function() {
    var color, curve, d, da, data, fill, first_point, height, last_point, line, ref, ref1, width, x, y;
    ref = this.props, width = ref.width, height = ref.height, data = ref.data, curve = ref.curve, fill = ref.fill, color = ref.color;
    ref1 = this.state, x = ref1.x, y = ref1.y;
    console.log('[LineChart.render] data=', data);
    line = d3.line().curve(curve ? d3.curveMonotoneX : d3.curveLinear).x(function(d) {
      return x(d.x);
    }).y(function(d) {
      return y(d.y);
    });
    d = line(data);
    return React.createElement("svg", {
      "className": 'line-chart',
      "style": {
        width: width,
        height: height,
        position: 'absolute',
        top: 0
      }
    }, (fill ? (first_point = {
      x: x.domain()[0],
      y: y.domain()[0]
    }, last_point = {
      x: x.domain()[1],
      y: y.domain()[0]
    }, da = line([first_point].concat(data).concat([last_point])), React.createElement("path", {
      "d": da,
      "fill": helpers.interpretColor(color, 0),
      "opacity": 0.2,
      "stroke": 'none'
    })) : void 0), React.createElement("path", {
      "d": d,
      "fill": 'none',
      "stroke": helpers.interpretColor(color, 0),
      "strokeWidth": 2
    }));
  };

  return LineChart;

})(Chart);
